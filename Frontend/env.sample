# Must match backend's BASE_PATH / FRONTEND_BASE_PATH.
VITE_BASE_URL = "listing-ccd/"

# Leave blank in production (same-origin, see src/api.js). Set for local dev
# when running the Vite dev server (5173) against a separately-running backend.
VITE_API_BASE_URI = "http://localhost:8081"
